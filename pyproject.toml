[project]
name = "world-news-map"
version = "0.1.0"

[project.optional-dependencies]
dev = ["pytest"]

[tool.setuptools]
py-modules = []

[tool.mutmut]
paths_to_mutate = ["scripts/download_events.py"]
test_command = "python -m pytest tests/ -x -q"
